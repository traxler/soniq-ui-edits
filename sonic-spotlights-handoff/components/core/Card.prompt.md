Surface panel on the void — hairline border, soft shadow, 12px radius.

```jsx
<Card>…</Card>
<Card raised interactive onClick={open}>…</Card>
<Card accent="voice">…</Card>   // purple left edge = active/reasoning item
```

`raised` lightens the surface; `interactive` adds hover lift; `accent` adds a left edge.
